services:
  db:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: cleardesk
      POSTGRES_PASSWORD: cleardesk
      POSTGRES_DB: cleardesk
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
